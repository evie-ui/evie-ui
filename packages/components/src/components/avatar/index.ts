import { AvatarRoot } from "./fragments/root";
import { AvatarImage } from "./fragments/image";
import { AvatarFallback } from "./fragments/fallback";

export const Avatar = {
  Root: AvatarRoot,
  Image: AvatarImage,
  Fallback: AvatarFallback,
};
