import { ProgressCircleIndicator } from "./fragments/indicator";
import { ProgressCircleRoot } from "./fragments/root";

export const ProgressCircle = {
  Root: ProgressCircleRoot,
  Indicator: ProgressCircleIndicator,
};
