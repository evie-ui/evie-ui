// import { SegmentedControl } from "../../segmented-control";

// type Props = React.ComponentProps<typeof SegmentedControl.Item> & { value: string };

// export const TabsTrigger = (props: Props) => {
//   return <SegmentedControl.Item {...props}>{props.children}</SegmentedControl.Item>;
// };
